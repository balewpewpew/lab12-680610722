interface TaskCardProps {
  id: string;
  title: string;
  description: string;
  isDone: boolean;
}

export type { TaskCardProps };

interface SideBar{
  userName: string;
  Type: string;
}

export type { SideBar };

interface footer{
  year: string;
  fullname: string;
  studentId: string;
}

export type {footer};